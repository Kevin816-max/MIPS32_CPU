ORI $1, $0, 0x5678   # 初始化正数
ORI $2, $0, 1        
SUB $2, $0, $2       # 初始化负数 (-1)
ORI $3, $0, 4        # 初始化移位量 (注意：这里把 4 存进了 $3)

SLLV $4, $1, $3      # 变量左移：移位量来自 $3
SRLV $5, $1, $3      # 变量逻辑右移
SRAV $6, $2, $3      # 变量算术右移

NOP                  # 结束